# FerreSystem — Dependencias Python
# Instalar: pip install -r requirements.txt

# ===== Adapters =====
openpyxl>=3.1.0       # ExcelAdapter: lectura de .xlsx

# ===== Firebase (pipeline/rotar_token.py) =====
firebase-admin>=6.0.0  # Firestore + Auth para rotación de token

# ===== Testing =====
pytest>=7.0.0
pytest-cov>=4.0.0
